server:
  port: 9091

spring:
  application:
    name: worker replication

  datasource:
    url: jdbc:postgresql://104.198.175.147:5432/postgres
    username: postgres
    password: bUbis{(2LAv{V;Mf
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: 172.31.250.176
      port: 6379

# Actuator - Health endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always